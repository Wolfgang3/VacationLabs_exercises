
(define (iter-f n)
  (define (iter f1 f2 f3 counter)
    (if (> counter n)
        f1
        (iter (fn f1 f2 f3) f1 f2 (+ counter 1))))
  (define (fn f1 f2 f3)
    (+ f1 (* 2 f2) (* 3 f3)))
  (if (< n 3)
      n
      (iter 2 1 0 3)))

