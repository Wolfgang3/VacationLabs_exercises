;; 
;=> 
;; from the above sqrt-stream procedure, there are reduntant computation.
; but when memo-proc is used, the terms in guess are calculated only once.  
; if momo-proc was not used then the two procedures would have been the same.
