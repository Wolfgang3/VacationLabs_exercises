// no. of bits required for the following:
=>
;since we hav a huffman tree it sorts the tree in descending order of symbols used
;so to encode the most frequent symbol it will require only 1 bit
; and to encode the least frequent symbol it wil take (n-1) bits