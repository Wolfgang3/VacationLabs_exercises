// Eva lu Ator
=> 
(car ''abracssdd)
;the above statement prints quote
; this is because the first quote takes it as a list, so by list the first value of the list is a quote which is returned by car

